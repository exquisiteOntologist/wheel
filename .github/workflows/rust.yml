



# MISSING UBUNTU DEPENDENCIES.
# Expect that identifying all missing dependencies will take too long.




# name: Rust

# on:
#   push:
#     branches: [ "main" ]
#   pull_request:
#     branches: [ "main" ]

# env:
#   CARGO_TERM_COLOR: always

# jobs:
#   build:

#     runs-on: ubuntu-latest

#     steps:
#     - uses: actions/checkout@v4
#     - name: Build
#       run: cargo build --release --verbose
#     - name: Run tests
#       run: cargo test --verbose
